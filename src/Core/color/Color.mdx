import { Meta, Canvas } from '@storybook/addon-docs'
import * as ColorStories from './Color.stories'

<Meta title="Core/Color" />

# Color

## 1. Primary Color

전반적인 UI 컴포넌트에 사용되는 색상이다.  
서비스 특성에 맞도록 원하는 색상으로 커스텀할 수 있다.

### 🎨 예시

<Canvas of={ColorStories.PrimaryColor} />

### 🛠️ 사용법

`index.scss` 파일에 하단 코드와 같이 `--primary-color` 색상을 정의한다.  
**HSL**값을 원칙으로 하며, H(hue, 색)와 S(saturation, 채도)를 커스텀 할 수 있다.  
L(lightness, 명도)을 기준으로 팔레트가 구성된다.  
기본 팔레트 색상은 하단 코드와 상단 팔레트 예시와 같다.

```css
:root {
  --primary-color: 222, 100%;
}
```

팔레트 색상을 사용할 땐 변수명을 사용한다.

```css
div {
  background-color: var(--Primary-Color-2);
}
```

### ✨ 규칙

- **6번**을 기본으로, 1번부터 5번은 색조, 7번부터 10번은 그림자로 사용한다.
- **6번**을 기본으로, Hover 또는 Focus인 경우 7번, Active인 경우 8번, 비활성화인 경우 **Gray 2번**을 사용한다.

## 2. Neutral Color

정보 전달과 위계를 나타내는 색상이다.  
 텍스트는 가독성을 위해 배경과의 명암비를 고려해야 한다.

### 🎨 예시

<Canvas of={ColorStories.NeutralColor} />

### 🛠️ 사용법

팔레트 색상을 사용할 땐 변수명을 사용한다.

```css
div {
  background-color: var(--Gray-Color-1);
}
h1 {
  color: var(--Gray-Color-10);
}
p {
  color: var(--Gray-Color-8);
}
```

### ✨ 규칙

- 기본 텍스트 색상은 8번, 타이틀 색상은 10번을 사용한다.
- 기본 비활성화 색상은 3번, 텍스트 비활성화 색상은 6번을 사용한다.
- 테두리 색상은 4~6번을 사용한다.
- 배경 색상은 0~2번을 사용한다.

## 3. System Color

시스템 상태를 보여줄 때 사용하는 색상이다.

### 🔴 Danger

오류, 금지, 위험, 삭제, 불가

<Canvas of={ColorStories.DangerColor} />

### 🟡 Warning

주의

<Canvas of={ColorStories.WarningColor} />

### 🟢 Success

성공, 진행

<Canvas of={ColorStories.SuccessColor} />

### 🔵 Information

정보, 강조

<Canvas of={ColorStories.InformationColor} />

### 🛠️ 사용법

팔레트 색상을 사용할 땐 변수명을 사용한다.

```css
div {
  background-color: var(--Danger-Color-0);
  color: var(--Danger-Color-6);
  border-color: var(--Danger-Color-1);
}
```

### ✨ 규칙

- **5번**을 기본으로, 6번은 텍스트, 0번은 배경, 1번은 테두리로 사용한다.
