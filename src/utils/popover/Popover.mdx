import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs'
import * as PopoverStories from './Popover.stories'
import { Popover } from '.'

<Meta of={PopoverStories} component={Popover} />

# Popover

하나의 컴포넌트 위에 다른 컴포넌트를 띄울 때 사용한다.

## 예시

<Canvas of={PopoverStories.Example} />

## API

<ArgTypes of={PopoverStories} />
