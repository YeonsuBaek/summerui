import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs'
import * as PortalStories from './Portal.stories'
import { Portal } from '.'

<Meta of={PortalStories} component={Portal} />

# Portal

날짜와 관련된 정보와 기능을 제공할 때 사용한다.

## 예시

<Canvas of={PortalStories.Example} />

## API

<ArgTypes of={PortalStories} />
