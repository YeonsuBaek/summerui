import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs'
import * as CheckboxStories from './Checkbox.stories'
import { Checkbox } from '.'

<Meta of={CheckboxStories} component={Checkbox} />

# Checkbox

사용자가 여러 개의 옵션 중 한 개 이상의 값을 선택할 때 사용한다.

## 예시

### 1. Size

<Canvas of={CheckboxStories.SizesOfCheckbox} />

## API

<ArgTypes of={CheckboxStories} />
