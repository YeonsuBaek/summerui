import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs'
import * as RadioGroupStories from './RadioGroup.stories'
import { RadioGroup } from '.'

<Meta of={RadioGroupStories} component={RadioGroup} />

# RadioGroup

사용자가 여러 개의 옵션 중 한 개 이상의 값을 선택할 때 사용한다.

## 예시

### Direction

<Canvas of={RadioGroupStories.Example} />

## API

<ArgTypes of={RadioGroupStories} />
