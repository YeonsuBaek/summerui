import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs'
import * as SwitchStories from './Switch.stories'
import { Switch } from '.'

<Meta of={SwitchStories} component={Switch} />

# Switch

사용자가 여러 개의 옵션 중 한 개 이상의 값을 선택할 때 사용한다.

## 예시

### 1. Size

<Canvas of={SwitchStories.SizesOfSwitch} />

### 2. Label

<Canvas of={SwitchStories.SwitchWithLabel} />

## API

<ArgTypes of={SwitchStories} />
