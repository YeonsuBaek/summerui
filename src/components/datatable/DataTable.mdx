import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs'
import * as DataTableStories from './DataTable.stories'
import { DataTable } from '.'

<Meta of={DataTableStories} component={DataTable} />

# DataTable

사용자가 여러 개의 옵션 중 한 개 이상의 값을 선택할 때 사용한다.

## 예시

<Canvas of={DataTableStories.Example} />

## API

<ArgTypes of={DataTableStories} />
