import { Meta, Canvas, Story, ArgTypes } from '@storybook/addon-docs'
import { Toast } from '.'
import * as ToastStories from './Toast.stories'
import { Button } from '../button'

<Meta title="Components/Toast" component={Toast} />

# Toast

사용자 행동에 대한 결과를 전달할 때 사용한다.

## 예시

<Canvas of={ToastStories.ToastExample} />

## API

<ArgTypes of={ToastStories} />
