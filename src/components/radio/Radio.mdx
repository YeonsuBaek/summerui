import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs'
import * as RadioStories from './Radio.stories'
import { Radio } from '.'

<Meta of={RadioStories} component={Radio} />

# Radio

사용자가 여러 개의 옵션 중 한 개의 값을 선택할 때 사용한다.

## 예시

### Size

<Canvas of={RadioStories.SizeOfRadio} />

### States

<Canvas of={RadioStories.StatesOfRadio} />

## API

<ArgTypes of={RadioStories} />
