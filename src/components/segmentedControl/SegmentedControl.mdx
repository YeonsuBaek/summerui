import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs'
import * as SegmentedControlStories from './SegmentedControl.stories'
import * as SegmentedControlButtonStories from './SegmentedControlButton.stories'
import { SegmentedControl } from '.'

<Meta of={SegmentedControlStories} component={SegmentedControl} />

# SegmentedControl

사용자가 여러 버튼이나 탭 중 하나를 선택할 때 사용한다.

## 예시

### 1. Size

<Canvas of={SegmentedControlStories.SizesOfSegmentedControl} />

### 2. Resizing

<Canvas of={SegmentedControlStories.ResizingOfSegmentedControl} />

## 하위 컴포넌트

### 1. Button

<Canvas of={SegmentedControlButtonStories.SegmentedControlButton} />

## API

<ArgTypes of={SegmentedControlStories} />
